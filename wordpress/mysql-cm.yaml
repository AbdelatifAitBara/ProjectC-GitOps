apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-config
data:  # store initial SQL commands
  init.sql: |
    CREATE USER 'wordpress'@'%' IDENTIFIED BY {{ .Data.data.WORDPRESS_DB_PASSWORD | quote }};
    CREATE DATABASE wordpress;
    GRANT ALL ON wordpress.* TO 'wordpress'@'%';
    FLUSH PRIVILEGES